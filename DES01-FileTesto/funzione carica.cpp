void carica
{
struct stud buffer;
int i;

//apertura file

for(i=0;i<dim;i++)
{
printf(“     “)
scanf(“%s”, buffer.cog);
//caricare altri campi

fwrite(&buffer, sizeof(struct stud), 1, f)
}
fclose; 
//chiusura file 
}

